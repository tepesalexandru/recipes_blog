<div class="col-md-8 mt-4">

	<div class="card p-3 mt-2">
		<div class="d-flex justify-content-between align-items-center">
			<div class="user d-flex flex-row ">
				<img src="{{imageSrc}}" width="32" height="32" class="user-img rounded-circle mr-2" style="object-fit: cover;">
				<div>
					<div>
						<small class="font-weight-bold text-primary">{{name}}</small>
					</div>
					<span>
						<small class="font-weight-bold">{{content}}</small>
					</span>
				</div>
			</div>
			<small>{{datePosted}}</small>
		</div>
	</div>
</div>
